# Weights & Biases configuration
WANDB_MODE: "online" # Change to "online" for logging and include appropriate ENTITY and PROJECT arguments
ENTITY: ""
PROJECT: ""
RUN_NAME: "Separate IPPO - Test POC"
OUTPUT_DIR: ""  # Optional local artifact directory. Empty => use ./<wandb_run_id>

# Environment configuration
ENV_NAME: "Craftax-Coop-Symbolic" # Can be changed to "Craftax-Coop-Symbolic", "Craftax-Coop-Pixels", "Craftax-MA-Symbolic", "Craftax-MA-Pixels"
DISABLE_REVIVE: true
TERMINATE_ON_ANY_DEATH: true
REVIVING_COOLDOWN_STEPS: 100
TEAMMATE_ALIVE_BONUS: 0.0
ALL_TEAM_ALIVE_BONUS: 0.01
DEAD_SELF_PENALTY_WEIGHT: 0.0

NUM_TEAMS: 2
TEAM_COMPOSITION: [1, 1, 2]  # [FORAGER, FORAGER, WARRIOR] # Specialization values: FORAGER=1, WARRIOR=2, MINER=3
USE_REDUCED_ACTION_SPACE: true  # If true, use actions 0-24 plus team-local GIVE target actions

# Model parameters
ALG_NAME: "seperate-ippo-rnn"
GRU_HIDDEN_DIM: 16
FC_DIM_SIZE: 8
ACTIVATION: "tanh"

# Training parameters
TOTAL_TIMESTEPS: 100000
NUM_ENVS: 2
NUM_STEPS: 8
NUM_MINIBATCHES: 2
UPDATE_EPOCHS: 2
GAE_LAMBDA: 0.8
GAMMA: 0.99
CLIP_EPS: 0.2
SCALE_CLIP_EPS: false
ENT_COEF: 0.01
VF_COEF: 0.5
AUX_COEF: 0.1
USE_TEAMMATE_AUXILIARY_LOSS: true
AUX_TEAMMATE_WEIGHT: 0.3333
ANNEAL_LR: true
LR: 0.0002
MAX_GRAD_NORM: 1.0
LR_WARMUP: 0.0
REW_SHAPING_HORIZON: 1000000

# Logging parameters
LOGGING_UPDATES_INTERVAL: 6  # Number of updates between logging phases
LOGGING_NUM_CALLS: 2  # Number of logging_step calls per phase
LOGGING_STEPS_PER_CALL: 400  # Number of env steps per logging_step call
LOGGING_THREADS: 1  # Number of env threads to log (must be <= NUM_ENVS) -> set to 1 for less computing time

# Video recording parameters
SAVE_VIDEO: true             # Whether to record and save videos during logging
VIDEO_LENGTH: 100  # Number of env steps per video (rollout continues from current state)
VIDEO_PIXEL_SIZE: 16  # Pixel size for rendering (7=agent, 16=medium, 64=large)
SAVE_HIDDEN_STATES: false     # Whether to save hidden state CSVs during logging (large files!)

# Seed configuration
NUM_SEEDS: 1
SEED: 0
